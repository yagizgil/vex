[package]
name = "vex"
version = "0.2.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"], optional = true }
serde_json = { version = "1.0.149", optional = true }

[features]
default = []
inspector = ["dep:serde", "dep:serde_json"]